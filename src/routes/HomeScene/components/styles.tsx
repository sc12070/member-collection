import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  wrapper: {
    marginVertical: 10,
    marginHorizontal: 10,
    flexDirection: 'row',
    zIndex: 0
  },
  dragging: {
    zIndex: 1
  }
})

export default styles
